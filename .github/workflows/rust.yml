name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build db + rabbitmq
      run: docker compose build rabbitmq timescale
    - name: run db + rabbitmq for testing
      run: docker compose up rabbitmq timescale -d
    - name: change dir to cargo
      run: cd options_analyzer
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
    - name: change dir to docker
      run: cd ..
    - name: shutdown containers
      run: docker compose down
